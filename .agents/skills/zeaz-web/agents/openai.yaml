interface:
  display_name: "Zeaz Web"
  short_description: "Repo-specific patterns and workflows for zeaz-web"
  default_prompt: "Use the zeaz-web repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true